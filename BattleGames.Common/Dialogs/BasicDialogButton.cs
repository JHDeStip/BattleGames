namespace Stip.BattleGames.Common.Dialogs
{
    // Be careful when adding or reordering values.
    // The order of the values in the enum define the order of the buttons in the dialog.
    public enum BasicDialogButton
    {
        Close,
        Yes,
        No
    }
}
